# Redis Caching
Learning how to use Redis for LLM application caching

## To spin up redis
```
docker compose up -d
```

Spin it down with:
```
docker compose down
```

## To install depdencies:
Using UV:
```
uv sync
```

Using pip:
```
pip install -r requirements.txt
```